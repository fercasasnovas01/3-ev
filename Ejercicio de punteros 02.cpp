//Programas sobre manejo de memoria //Aquí dice el tipo de programa
#include<stdio.h> //Aquí dice la libreria de las funciones
int main(){ //Marcamos el inicio del programa
    int x=3; //Definimos la variable "x" y le asignamos un valor
    int salir; //Definimos la variable salir como función de retorno
    printf("x= %i",x); //Guardamos un segmento de la memoria para guardar números enteros y ponemos "x"
    scanf("%c",&salir);//Guardamos un segmento de la memoria para guardar letras, ponemos salir y hacemos que lo saque por pantalla
}
