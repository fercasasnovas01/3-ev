//Programas sobre manejo de memoria //Aquí dice el tipo de programa
#include<iostream>//Aquí dice la libreria de las funciones
using namespace std; //Esta función permite usar solo cout o cin en vez de std::cout y std::cin respectivamente 
int main(){ //Marcamos el inicio del programa
    int x=3; //Definimos la variable "x" y le asignamos un valor
    int salir; //Definimos la variable salir como función de retorno
    cout<<"x= "<<x; //Le pedimos al programa que saque por pantalla el valor de "x"
    cin>>salir; //Activamos la función de retorno
}
